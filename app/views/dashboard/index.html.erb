
<div class="images">
    <% @links.each do |link| %>
    <div class="box">
      <%= image_tag "#{link}" %>
      <%= button_to "Favorite", favorites_path(favorite: link), class: 'btn btn-info btn-md btn-block text-center' %>
    </div>
    <% end %>
</div>
